---
- hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Run the python file
      command: python /var/lib/awx/projects/_6__ansi/Reading_Excel_file.py
